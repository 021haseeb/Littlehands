# Animations / Transitions Update

- [ ] Add global scroll-reveal system (IntersectionObserver) in `index.js`
- [ ] Add reveal animation CSS utilities in `index.css`
- [ ] Add `.reveal` classes (and optional `data-delay`) to key layout elements in `index.html`
- [ ] Quick sanity checks (no slider/lightbox/FAQ breakage)

